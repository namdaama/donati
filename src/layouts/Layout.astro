---
import '../styles/global.css';
import CustomCursorStarTheme from '../components/effects/CustomCursor-StarTheme.astro';
import { siteConfig } from '../config/site';

export interface Props {
  title: string;
  description?: string;
}

const { title, description = 'サイエンス アンド スペース ラボ DONATI - 科学の楽しさを伝え、世界をワクワクでいっぱいにする' } = Astro.props;
const canonicalUrl = new URL(Astro.url.pathname, siteConfig.url);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | サイエンス アンド スペース ラボ DONATI</title>
    
    <!-- OGP -->
    <meta property="og:title" content={`${title} | サイエンス アンド スペース ラボ DONATI`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl.toString()} />
    <meta property="og:locale" content="ja_JP" />

    <!-- Canonical -->
    <link rel="canonical" href={canonicalUrl.toString()} />
    
    <!-- Global Styles -->
  </head>
  <body class="min-h-screen flex flex-col">
    <CustomCursorStarTheme />
    <slot />
  </body>
</html>